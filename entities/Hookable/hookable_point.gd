extends Node2D


# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass
	#var debug_shape = ColorRect.new()
	#debug_shape.color = Color(1, 0, 0, 0.5)  # Semi-transparent red
	#debug_shape.size = Vector2(8, 8)  # Adjust to see it clearly
	#debug_shape.position = -debug_shape.size / 2  # Center it
	#add_child(debug_shape)


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
